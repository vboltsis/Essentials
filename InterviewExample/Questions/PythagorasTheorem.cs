namespace InterviewExample;

//Given the pythagoras theorem, write the algorithm that computes it
internal class PythagorasTheorem
{
    internal static double GetHypotenuse(double a, double b)
    {
        return Math.Sqrt(a * a + b * b);
    }
}
